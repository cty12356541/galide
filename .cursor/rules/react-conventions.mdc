---
description: React 组件规范
globs: **/*.tsx
alwaysApply: false
---
<!-- 注:本文件镜像 `.style-spec/layers/renderer/conventions.yaml`,修改请同步源文件,避免双份规约漂移。 -->

# React 组件规范

## 组件定义

- **函数组件 + Hooks**,禁止 class 组件
- 文件名与组件名一致: `CharacterCardEditor.tsx` 导出 `CharacterCardEditor`
- 纯展示组件放 `features/*/components/`,数据逻辑通过 props 传入

## Hooks 封装

- 有状态的逻辑封装为 `useXxx` hook
- 每个 hook 单独文件: `useProject.ts`, `useScriptEditor.ts`
- IPC 调用必须封装为 custom hook,禁止在组件内直接调用 IPC

## 状态管理

| 状态类型 | 管理方式 |
|---------|---------|
| 组件内部状态 | `useState` / `useReducer` |
| 跨组件共享 UI 状态 | Zustand store |
| AI 任务 / Git 状态 | React Query |

## 关键 Hooks 命名

- `useProject()` — 当前打开的项目
- `useScriptEditor()` — CodeMirror 编辑器实例
- `useFlowView()` — React Flow 实例
- `usePreview()` — PixiJS 预览实例
- `useGitStatus()` — Git 工作区状态
- `useAiTask()` — AI 任务队列

## CSS

- **Tailwind class 优先**
- 仅在 Tailwind 无法表达的复杂样式使用 scoped CSS
